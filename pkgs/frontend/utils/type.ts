export type ListInfo = {
  app: string;
  blockchain: string;
  cta: string;
  icon: string;
  image: string;
  message: string;
  notification: {
    title: string;
    body: string;
  };
  secret: string;
  sid: string;
  title: string;
  url: string;
};
